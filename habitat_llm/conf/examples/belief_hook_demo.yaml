demo:
  description: "Toy configuration for belief-aware planner demo."
  decision_hooks:
    enable: True
    cbwm_enabled: True
    concept_confidence_threshold: 0.6
    divergence_metric: "concept_js_divergence"
    divergence_threshold: 0.25
    correction_divergence_threshold: 0.4
    l2d_action:
      enable: True
      divergence_threshold: 0.55
      action: "ListenToDisambiguate"
